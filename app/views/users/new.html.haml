%h1 Sign-up

= form_tag users_path do
    = label :user, :user_id, 'User-ID'
    = text_field :user, 'user_id', :id => 'signup_user'

    = label :user, :email, 'EMail'
    = text_field :user, 'email', :id => 'signup_email'
    
    = submit_tag 'Create Account', :id => 'signup_create'
